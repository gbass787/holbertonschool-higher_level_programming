-- Script that creates a table called 'first_table' in the current database
-- Description for 'first_table': id 'INT' and name 'VARCHAR(256)'
CREATE TABLE IF NOT EXISTS first_table(
    id INT,
    name VARCHAR(256));